import React from 'react';

const style = {
  display: 'flex',
  width: '100%'
}

export default class Header extends React.Component {
  render() {
    return (
      <header style={ style }>
        mMm
      </header>
    )
  }
};